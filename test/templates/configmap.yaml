apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-configmap
data:
  myvalue: "Hello World"
  coffee: {{ .Values.favoriteDrink }}
{{- if eq .Values.favoriteDrink "coffee" }}
  mug: "true"
{{ end }}
